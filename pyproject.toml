[project]
name = "dummy-api-mapping-studio"
version = "0.1.0"
description = "Safe sample project for OpenAPI field mapping."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
